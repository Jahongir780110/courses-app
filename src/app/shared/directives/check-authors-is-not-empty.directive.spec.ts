import { CheckAuthorsIsNotEmptyDirective } from './check-authors-is-not-empty.directive';

describe('CheckAuthorsIsNotEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckAuthorsIsNotEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
